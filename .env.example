# Retail Vision Intelligence System — variáveis de ambiente
# Copia este ficheiro para .env e preenche com a tua chave.
#   cp .env.example .env
#
# Obtém uma chave gratuita (sem cartão) em: https://aistudio.google.com → Get API Key
# NUNCA commites o .env real — está no .gitignore.

GEMINI_API_KEY=cola_aqui_a_tua_chave
